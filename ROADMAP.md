# Дорожная карта Ellochka-Zig (v3 — по официальному help DIKAR v7)

## Статус на 04.08.2026, вечер

### Готово (MVP v0.1)
- Системные (1/7): EXIT, STOP, MEMC (только вариант простых переменных)
- Управляющие (7/7): RADI, GRDS, ORUP, ORDN, GOTO, ESLI, SLEP
- Ввод/вывод (6/12): CLSC, CFON, CSIM, STRO, STLB, LIST, VVOD
- Присваивание (частично): вариант 1 (A=expr, A[i]=expr, A[i,j]=expr),
  вариант 2/3 (неявные циклы A[]=, A[,]=) написаны в коде, но требуют SIZE
- Математика через &: все 13 из 13 (RAN, SIN, COS, TAN, ASN, ACS, ATN,
  EXP, LOG, INT, FRC, ABS, SGN, SQR)
- $0-$9: чтение/вывод как текстовые переменные произвольной длины
- Метки (@LABEL) и переходы GOTO/ESLI
- ArenaAllocator на строку, корректный deinit for labels

## ТОЧНЫЙ состав языка (по официальному help, 62 оператора)

### СИСТЕМНЫЕ (7): EXIT*, STOP*, DOSC, SIZE, UMEM, MEMC*, CURR
### УПРАВЛЯЮЩИЕ (7): RADI*, GRDS*, ORUP*, ORDN*, GOTO*, ESLI*, SLEP*
### ВВОД/ВЫВОД (12): GRAF, TEXT, CLSC*, CFON*, CSIM*, STRO*, STLB*,
                      KEYS, WAIT, LIST*, VVOD*, DATA
### ГРАФИЧЕСКИЕ (9): PIXL, RDOT, CVET, LINE, RAMA, MOVE, KRUG, PAIN, SBMP
### ФАЙЛОВЫЕ (9): LENF, UDAL, PATH, FILE, FREE, READ, TYPE, GETF, PUTF
### МАТЕМАТИЧЕСКИЕ (8): LIRA, POLI, DTRM, APRO, REAK, USER, TRAN, NTGR
### ПРОЧИЕ (10): MINA, MAXA, SUMA, SORT, INCR, DECR, FIND, DLIN, MENU, JULD

(* — уже реализовано; итог: 20 из 62 полностью, +частично MEMC/присваивание)

## Уточнения из официального текста (важно учесть при доработке!)

### Присваивание — 6 вариантов (сейчас реализован только №1)
1. A=expr — готово
2. A[]=expr — неявный цикл по одномерному индексу (?), требует SIZE — код есть, не активен
3. A[,]=expr — неявный цикл по двум индексам (?1,?2), требует SIZE — код есть, не активен
4. A:константа — короткая форма с плавающей точкой (A:-6.02e-23) — НЕ РЕАЛИЗОВАНО
5. A:$?[индекс] — присвоить код символа из строки — НЕ РЕАЛИЗОВАНО
6. A#$0 — вычислить выражение, находящееся в текстовой переменной, и присвоить
   результат — НЕ РЕАЛИЗОВАНО (интересный кейс: интерпретатор внутри интерпретатора)

### Присваивание строк (текстовых переменных) — НЕ РЕАЛИЗОВАНО
- $I=$J+'текст'+A (конкатенация через + текстовых переменных, констант и чисел)
- $?[индекс]=выражение — присвоить код символа внутри строки по индексу

### Строковые переменные — важное уточнение!
- $0-$9 — переменные ПРОИЗВОЛЬНОЙ длины (до 1024 символов) — уже реализовано
- $A-$Z — это элементы СТРОКОВОГО МАССИВА фиксированной длины 75 символов
  (до 850 элементов), т.е. $A эквивалентно $[1], $B эквивалентно $[2] и т.д.
  Это ДРУГАЯ структура данных, не 26 отдельных переменных! Нужен единый массив
  static_strings[850][75], а буква просто маппится на индекс.

### Метакоманды (!nul, !one, !err) — НЕ РЕАЛИЗОВАНО
- !nul, !one, !err в начале строки управляют поведением при 0/0 в выражениях
  (по умолчанию !err — выдать ошибку). Нужно добавить флаг в InterpreterState
  и проверять его в expr.evaluate при делении.

### Условие ESLI — обнаружен второй режим сравнения (**), не только (%%)!
- ESLI X ** {Y,Z} C — X принадлежит диапазону [Y,Z] включительно
- ESLI X ** }Y,Z{ C — X НЕ принадлежит диапазону (Y,Z) исключая границы
- ESLI P%%F C — сравнение ТЕКСТОВЫХ переменных/констант (P,F), не только чисел!
  Текущая реализация execEsli поддерживает только числовые %%, нужно добавить:
  1) сравнение строк, 2) режим ** с диапазоном {}/{}

## Приоритеты реализации (обновлено, финальная версия)

### Приоритет 1 — динамические массивы (открывает варианты 2,3 присваивания)
- [ ] SIZE (все 3 формы: строковый/1D/2D)
- [ ] UMEM
- [ ] CURR
- [ ] MEMC полностью ($$, 1M, 2K)

### Приоритет 2 — короткие формы присваивания и строки
- [ ] INCR / DECR
- [ ] A:константа (вариант 4)
- [ ] A:$?[индекс] (вариант 5)
- [ ] A#$текст (вариант 6)
- [ ] Присваивание $I=... с конкатенацией через +
- [ ] $?[индекс]=expr (посимвольная запись в строку)
- [ ] $A-$Z как элементы строкового массива (переработать модель данных!)

### Приоритет 3 — расширение ESLI и строковых функций
- [ ] ESLI P%%F C (сравнение строк)
- [ ] ESLI X ** {Y,Z}/}Y,Z{ C (диапазоны)
- [ ] %MID, %CHR, %DATE, %TIME
- [ ] DLIN, FIND
- [ ] DATA A;B;@label

### Приоритет 4 — ввод/вывод и меню
- [ ] KEYS, WAIT
- [ ] MENU (многостраничное меню)

### Приоритет 5 — файловые операторы (9 шт.)
- [ ] LENF, UDAL, PATH, FILE, FREE, READ, TYPE, GETF, PUTF

### Приоритет 6 — графика (требует выбора backend: WinAPI GDI vs raylib/SDL2 FFI)
- [ ] GRAF/TEXT реальные режимы
- [ ] PIXL, RDOT, CVET, LINE, RAMA, MOVE, KRUG, PAIN, SBMP

### Приоритет 7 — математические операторы (самые сложные алгоритмически)
- [ ] MINA, MAXA, SUMA, SORT (простые, можно раньше)
- [ ] LIRA (СЛАУ — метод Гаусса)
- [ ] POLI (вычисление полинома)
- [ ] DTRM (определитель матрицы)
- [ ] APRO (полиномиальная аппроксимация — МНК)
- [ ] REAK (аппроксимация A*X^B*exp(-C*X) — нелинейная регрессия)
- [ ] NTGR (численное интегрирование)
- [ ] TRAN (система нелинейных уравнений, до 9 шт.)
- [ ] USER (аппроксимация произвольной пользовательской функцией — нужен
      мини-интерпретатор выражений из текстовой переменной, пересекается
      с вариантом присваивания №6 A#$текст)

### Приоритет 8 — метакоманды и календарь
- [ ] !nul / !one / !err
- [ ] JULD

## Технический долг
- [ ] Unit-тесты для execAssignment, execEsli, expr.evaluate
- [ ] Байтовый лимит строки 75 символов (уже встречалось на кириллице)
- [ ] main.zig: коды возврата процесса при ошибках
- [ ] Архитектурное решение по графике (WinAPI GDI напрямую — вероятно
      проще для Windows-only цели, чем тащить SDL2/raylib)
- [ ] DIKAR-редактор и IDE (F1-F10, трассировка, ручной режим) — отдельная
      большая фича, не входит в интерпретатор как таковой, можно отложить
      на самый конец или не делать вообще (это уже не язык, а среда разработки)

## Обновление статуса — вечер 04.08.2026

### Готово и ПРОВЕРЕНО тестом test_size.ell
- SIZE (все 3 формы: строковый/1D/2D) — работает, включая неявные циклы A[]=, A[,]=
- UMEM (уничтожение по категориям 1/2/3)
- CURR (1K/2K/3K по семантике официального help DIKAR v7)
- MEMC (все 4 варианта: без аргументов, $$, 1M, 2K)
- INCR / DECR

Покрытие языка теперь: 25 из 62 операторов полностью реализовано
(было 20, добавили SIZE, UMEM, CURR, INCR, DECR).

### Следующий шаг по дорожной карте — Приоритет 2 остаток:
- A:константа (короткая форма присваивания с плавающей точкой)
- A:$?[индекс] (код символа из строки)
- A#$текст (вычисление выражения из текстовой переменной)
- Присваивание строк $I=... с конкатенацией через +
- $?[индекс]=expr (посимвольная запись в строку)
- $A-$Z как элементы строкового массива (теперь static_strings готов в state.zig,
  осталось подключить парсинг $A-$Z в lexer/expr и чтение/запись в statement.zig)

## Обновление статуса — 04.08.2026, продолжение вечера

### Готово и ПРОВЕРЕНО тестом test_strings.ell
- Косвенная адресация $A-$Z (индекс = текущее значение одноимённого скаляра)
- Присваивание строк с конкатенацией через + ($I=$J+' '+$K, числа->строка)
- Чтение кода символа: A:$?[индекс]
- Запись кода символа: $?[индекс]=выражение
- Короткая форма присваивания A:константа (алиас для A=выражение)
- Вычисление выражения из строки: A#$текст

Покрытие языка теперь: 25 из 62 операторов + полный набор из 6 вариантов
присваивания (было реализовано только 1 из 6).

### Следующий шаг по дорожной карте — выбираем один из блоков:
- Приоритет 3: DLIN, FIND, %MID/%CHR/%DATE/%TIME, DATA, ESLI с диапазонами
  и сравнением строк (P%%F)
- Приоритет 4: KEYS, WAIT, MENU
- Приоритет 5: файловые операторы (9 шт.)
- Приоритет 6: графика (требует выбора backend)
- Приоритет 7: математические операторы (LIRA, POLI, DTRM, APRO, REAK, ...)

## Разбор пробелов для запуска sample.ela — 04.08.2026 вечер

Полный разбор оригинального демо-файла автора языка показал, какие
операторы реально нужны для его выполнения (помимо уже готового ESLI,
SIZE, DLIN, FIND, %CHR, INCR/DECR, присваивание всех 6 вариантов).

### Группа 1 — быстрые победы (低 effort, без внешних зависимостей)
- [ ] KEYS A       — код нажатой клавиши без ожидания (0 если не нажата)
- [ ] WAIT [A[#]]  — ждать нажатия клавиши, опционально в переменную
- [ ] SUMA D;S      — сумма элементов одномерного массива
- [ ] MINA D;I      — индекс минимального элемента
- [ ] MAXA D;I      — индекс максимального элемента
- [ ] DATA A;B;@label — заполнить строковый массив из строк программы

Для KEYS/WAIT нужен неблокирующий (KEYS) и блокирующий (WAIT) опрос
клавиатуры на Windows — реализуется через consoleapi (_kbhit/_getch
аналоги) или WinAPI ReadConsoleInput, без графики.

### Группа 2 — файловые операторы (умеренный объём, стандартный I/O)
- [ ] LENF F;L     — длина файла
- [ ] UDAL F       — удаление файла
- [ ] PATH F       — полный путь из маски/имени
- [ ] FILE F;N     — поиск файлов по шаблону в текстовый массив
- [ ] FREE D;L     — свободное место на диске
- [ ] SORT $;N;F / SORT A;N;F — сортировка строкового/числового массива
- [ ] TYPE F;список — запись в текстовый файл (как LIST, но в файл)
- [ ] PUTF/GETF F;... — чтение/запись двоичных файлов

Все реализуемы через std.fs без сторонних зависимостей.

### Группа 3 — интерфейс
- [ ] MENU N;L;S;C;F — многостраничное текстовое меню
  (зависит от Группы 1: KEYS/WAIT для навигации)

### Группа 4 — самое трудное: полноценная графика (9 операторов)
- [ ] GRAF (реальный режим 640x480x16, не заглушка)
- [ ] PIXL, RDOT, CVET, LINE, RAMA, MOVE, KRUG, PAIN, SBMP

sample.ela почти наполовину состоит из рисования (раздел @grafika,
радуга @raduga, счётчик точек по цвету через RDOT). Без реального
рендеринга эта часть физически не может работать.

Архитектурное решение: создание окна и растровая графика на Windows
без внешних зависимостей — через WinAPI (CreateWindow + GDI:
SetPixel/Rectangle/Ellipse/BitBlt для SBMP). Это на порядок больше
кода, чем всё, что сделано в проекте до сих пор — отдельный крупный
этап, не встраиваемый "по пути".

### Группа 5 — численные методы (математический блок)
- [ ] REAK X;Y;A;B;C;R;F — нелинейная регрессия A*X^B*exp(-C*X)
  (используется в разделе @matem для аппроксимации точек)
  Требует итерационного численного метода (напр. Гаусса-Ньютона).

### Рекомендация по порядку
1. Группа 1 (KEYS/WAIT/SUMA/MINA/MAXA/DATA) — открывает разделы
   @grafika (частично, счётчик точек) и меню @begin.
2. Группа 2 (файловые) — открывает раздел @files целиком.
3. Группа 3 (MENU) — делает рабочим главное меню @begin полностью.
4. Группа 5 (REAK) — открывает раздел @matem (кроме самого графика).
5. Группа 4 (графика) — отдельный крупный этап, открывает @grafika
   и графическую часть @matem/@raznoe полностью; sample.ela без него
   не может пройти целиком, но с группами 1-3-5 пройдёт минимум
   разделы @begin (меню), @files, и @matem (без вывода графика).

## Обновление статуса — 04.08.2026, ночь

### Готово и ПРОВЕРЕНО тестом test_keys_data.ell
- KEYS A — неблокирующая проверка клавиатуры (PeekConsoleInput)
- WAIT [A[#]] — блокирующее ожидание клавиши без Enter (raw-режим консоли,
  подтверждено реальным нажатием — код 13 для Enter получен без ввода
  дополнительного подтверждения)
- SUMA D;S, MINA D;I, MAXA D;I — first-match при равных значениях подтверждён
- DATA A;B;C — прямая индексация строкового массива из строк программы
  (метка @label работает корректно)

Покрытие языка теперь: 31 из 62 операторов реализовано полностью.

### Оставшиеся пробелы для полного sample.ela
- Группа 2 (файловые): LENF, UDAL, PATH, FILE, FREE, SORT, TYPE, PUTF/GETF
- Группа 3 (интерфейс): MENU (теперь разблокирована, т.к. KEYS/WAIT готовы)
- Группа 5 (математика): REAK — нелинейная регрессия
- Группа 4 (графика, самая трудная): GRAF реальный + PIXL, RDOT, CVET,
  LINE, RAMA, MOVE, KRUG, PAIN, SBMP — требует WinAPI GDI, отдельный этап



## Обновление статуса — Группа 2 (файловые операторы) завершена

### Готово и ПРОВЕРЕНО тестами test_files.ell / test_file_glob.ell
- LENF F;L — длина файла, -1 если не найден (подтверждено: 18 байт точно)
- UDAL F — удаление файла
- PATH F — абсолютный путь директории с завершающим \
- FILE F;N — поиск по DOS-маске (*,?), запись в строковый массив с 1
- FREE D;L — свободное место на диске через WinAPI GetDiskFreeSpaceExA
- SORT $/$$/A;N;F — сортировка строк/чисел, направление по знаку F
- TYPE F;список/1M/2K/$$;P;S — запись в файл (все 4 варианта)
- PUTF/GETF F;1M;D;B;L — интерливинг для числовых массивов (D=4/f32)
- PUTF/GETF F;P;R;B — текстовый буфер фиксированной длины

Архитектурная особенность этой сборки Zig (0.16.0-dev): std.fs.cwd()
не существует, все файловые операции идут через std.Io.Dir/std.Io.File
с явным параметром io. Все 6 операторов переписаны под этот API.

Покрытие языка теперь: 39 из 62 операторов реализовано и проверено.

### Оставшиеся пробелы
- Группа 3 (MENU) — разблокирована (KEYS/WAIT готовы)
- Группа 5 (REAK) — нелинейная регрессия для @matem
- Группа 4 (графика, 9 операторов) — отдельный крупный этап, WinAPI GDI
- PUTF/GETF варианты 2K (двумерные массивы) и A (простая переменная)
  отложены как договаривались


## Обновление статуса — Группа 3 (MENU) завершена

### Готово и ПРОВЕРЕНО тестом test_menu.ell (в cmd.exe)
- MENU N;L;S;C;F — многостраничное меню, подтверждено в реальном cmd.exe:
    - Стрелки ↑/↓ — навигация со сквозной прокруткой между страницами
    - PgUp/PgDn — прямое переключение страниц
    - Enter — выбор (F=номер пункта)
    - Esc — отмена (F=0)
    - Инверсия цвета для выделения текущего пункта
    - drainPendingInput() — защита от фантомных нажатий при запуске
    - stdout.flush() после каждого кадра — исправлена проблема невидимого
      вывода при блокирующем ожидании клавиши

ВАЖНО: ANSI-операторы (CSIM/CFON/STRO/STLB/MENU) корректно работают
в cmd.exe и Windows Terminal, но НЕ гарантированно корректно отображаются
во встроенных консолях IDE (например, IntelliJ IDEA) — инверсия цвета
и позиционирование курсора там может отображаться некорректно.
Тестировать интерактивные функции нужно в отдельном cmd.exe/Windows Terminal.

Покрытие языка теперь: 40 из 62 операторов реализовано и проверено.

### Оставшиеся пробелы
- Группа 5 (REAK) — нелинейная регрессия для @matem
- Группа 4 (графика, 9 операторов) — отдельный крупный этап, WinAPI GDI
- PUTF/GETF варианты 2K и A — отложены
