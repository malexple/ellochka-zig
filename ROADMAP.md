# Дорожная карта Ellochka-Zig (v3 — по официальному help DIKAR v7)

## Статус на 04.08.2026, вечер

### Готово (MVP v0.1)
- Системные (1/7): EXIT, STOP, MEMC (только вариант простых переменных)
- Управляющие (7/7): RADI, GRDS, ORUP, ORDN, GOTO, ESLI, SLEP
- Ввод/вывод (6/12): CLSC, CFON, CSIM, STRO, STLB, LIST, VVOD
- Присваивание (частично): вариант 1 (A=expr, A[i]=expr, A[i,j]=expr),
  вариант 2/3 (неявные циклы A[]=, A[,]=) написаны в коде, но требуют SIZE
- Математика через &: все 13 из 13 (RAN, SIN, COS, TAN, ASN, ACS, ATN,
  EXP, LOG, INT, FRC, ABS, SGN, SQR)
- $0-$9: чтение/вывод как текстовые переменные произвольной длины
- Метки (@LABEL) и переходы GOTO/ESLI
- ArenaAllocator на строку, корректный deinit for labels

## ТОЧНЫЙ состав языка (по официальному help, 62 оператора)

### СИСТЕМНЫЕ (7): EXIT*, STOP*, DOSC, SIZE, UMEM, MEMC*, CURR
### УПРАВЛЯЮЩИЕ (7): RADI*, GRDS*, ORUP*, ORDN*, GOTO*, ESLI*, SLEP*
### ВВОД/ВЫВОД (12): GRAF, TEXT, CLSC*, CFON*, CSIM*, STRO*, STLB*,
                      KEYS, WAIT, LIST*, VVOD*, DATA
### ГРАФИЧЕСКИЕ (9): PIXL, RDOT, CVET, LINE, RAMA, MOVE, KRUG, PAIN, SBMP
### ФАЙЛОВЫЕ (9): LENF, UDAL, PATH, FILE, FREE, READ, TYPE, GETF, PUTF
### МАТЕМАТИЧЕСКИЕ (8): LIRA, POLI, DTRM, APRO, REAK, USER, TRAN, NTGR
### ПРОЧИЕ (10): MINA, MAXA, SUMA, SORT, INCR, DECR, FIND, DLIN, MENU, JULD

(* — уже реализовано; итог: 20 из 62 полностью, +частично MEMC/присваивание)

## Уточнения из официального текста (важно учесть при доработке!)

### Присваивание — 6 вариантов (сейчас реализован только №1)
1. A=expr — готово
2. A[]=expr — неявный цикл по одномерному индексу (?), требует SIZE — код есть, не активен
3. A[,]=expr — неявный цикл по двум индексам (?1,?2), требует SIZE — код есть, не активен
4. A:константа — короткая форма с плавающей точкой (A:-6.02e-23) — НЕ РЕАЛИЗОВАНО
5. A:$?[индекс] — присвоить код символа из строки — НЕ РЕАЛИЗОВАНО
6. A#$0 — вычислить выражение, находящееся в текстовой переменной, и присвоить
   результат — НЕ РЕАЛИЗОВАНО (интересный кейс: интерпретатор внутри интерпретатора)

### Присваивание строк (текстовых переменных) — НЕ РЕАЛИЗОВАНО
- $I=$J+'текст'+A (конкатенация через + текстовых переменных, констант и чисел)
- $?[индекс]=выражение — присвоить код символа внутри строки по индексу

### Строковые переменные — важное уточнение!
- $0-$9 — переменные ПРОИЗВОЛЬНОЙ длины (до 1024 символов) — уже реализовано
- $A-$Z — это элементы СТРОКОВОГО МАССИВА фиксированной длины 75 символов
  (до 850 элементов), т.е. $A эквивалентно $[1], $B эквивалентно $[2] и т.д.
  Это ДРУГАЯ структура данных, не 26 отдельных переменных! Нужен единый массив
  static_strings[850][75], а буква просто маппится на индекс.

### Метакоманды (!nul, !one, !err) — НЕ РЕАЛИЗОВАНО
- !nul, !one, !err в начале строки управляют поведением при 0/0 в выражениях
  (по умолчанию !err — выдать ошибку). Нужно добавить флаг в InterpreterState
  и проверять его в expr.evaluate при делении.

### Условие ESLI — обнаружен второй режим сравнения (**), не только (%%)!
- ESLI X ** {Y,Z} C — X принадлежит диапазону [Y,Z] включительно
- ESLI X ** }Y,Z{ C — X НЕ принадлежит диапазону (Y,Z) исключая границы
- ESLI P%%F C — сравнение ТЕКСТОВЫХ переменных/констант (P,F), не только чисел!
  Текущая реализация execEsli поддерживает только числовые %%, нужно добавить:
  1) сравнение строк, 2) режим ** с диапазоном {}/{}

## Приоритеты реализации (обновлено, финальная версия)

### Приоритет 1 — динамические массивы (открывает варианты 2,3 присваивания)
- [ ] SIZE (все 3 формы: строковый/1D/2D)
- [ ] UMEM
- [ ] CURR
- [ ] MEMC полностью ($$, 1M, 2K)

### Приоритет 2 — короткие формы присваивания и строки
- [ ] INCR / DECR
- [ ] A:константа (вариант 4)
- [ ] A:$?[индекс] (вариант 5)
- [ ] A#$текст (вариант 6)
- [ ] Присваивание $I=... с конкатенацией через +
- [ ] $?[индекс]=expr (посимвольная запись в строку)
- [ ] $A-$Z как элементы строкового массива (переработать модель данных!)

### Приоритет 3 — расширение ESLI и строковых функций
- [ ] ESLI P%%F C (сравнение строк)
- [ ] ESLI X ** {Y,Z}/}Y,Z{ C (диапазоны)
- [ ] %MID, %CHR, %DATE, %TIME
- [ ] DLIN, FIND
- [ ] DATA A;B;@label

### Приоритет 4 — ввод/вывод и меню
- [ ] KEYS, WAIT
- [ ] MENU (многостраничное меню)

### Приоритет 5 — файловые операторы (9 шт.)
- [ ] LENF, UDAL, PATH, FILE, FREE, READ, TYPE, GETF, PUTF

### Приоритет 6 — графика (требует выбора backend: WinAPI GDI vs raylib/SDL2 FFI)
- [ ] GRAF/TEXT реальные режимы
- [ ] PIXL, RDOT, CVET, LINE, RAMA, MOVE, KRUG, PAIN, SBMP

### Приоритет 7 — математические операторы (самые сложные алгоритмически)
- [ ] MINA, MAXA, SUMA, SORT (простые, можно раньше)
- [ ] LIRA (СЛАУ — метод Гаусса)
- [ ] POLI (вычисление полинома)
- [ ] DTRM (определитель матрицы)
- [ ] APRO (полиномиальная аппроксимация — МНК)
- [ ] REAK (аппроксимация A*X^B*exp(-C*X) — нелинейная регрессия)
- [ ] NTGR (численное интегрирование)
- [ ] TRAN (система нелинейных уравнений, до 9 шт.)
- [ ] USER (аппроксимация произвольной пользовательской функцией — нужен
      мини-интерпретатор выражений из текстовой переменной, пересекается
      с вариантом присваивания №6 A#$текст)

### Приоритет 8 — метакоманды и календарь
- [ ] !nul / !one / !err
- [ ] JULD

## Технический долг
- [ ] Unit-тесты для execAssignment, execEsli, expr.evaluate
- [ ] Байтовый лимит строки 75 символов (уже встречалось на кириллице)
- [ ] main.zig: коды возврата процесса при ошибках
- [ ] Архитектурное решение по графике (WinAPI GDI напрямую — вероятно
      проще для Windows-only цели, чем тащить SDL2/raylib)
- [ ] DIKAR-редактор и IDE (F1-F10, трассировка, ручной режим) — отдельная
      большая фича, не входит в интерпретатор как таковой, можно отложить
      на самый конец или не делать вообще (это уже не язык, а среда разработки)
