# Дорожная карта Ellochka-Zig

## Статус на 04.08.2026

### Готово (MVP v0.1)
- Системные: EXIT, STOP, MEMC (простые переменные)
- Управляющие: RADI, GRDS, ORUP, ORDN, GOTO, ESLI, SLEP
- Ввод/вывод: LIST, VVOD, CLSC, CFON, CSIM, STRO, STLB
- Присваивание: скаляры A=expr, массивы A[i]/A[i,j] (без SIZE пока не выделяются)
- Математика через &: RAN, SIN, COS, TAN, ASN, ACS, ATN, EXP, LOG, INT, FRC, ABS, SGN, SQR
- Динамические строки $0-$9: чтение (VVOD $N) и вывод (LIST ...; $N)
- Метки (@LABEL) и переходы по GOTO/ESLI
- Управление памятью: ArenaAllocator на каждую строку, errdefer в Program.load,
  освобождение ключей labels в Program.deinit

## Следующий этап (вечер 04.08 / v0.2)

### Приоритет 1 — открывает динамические массивы
- [ ] SIZE — выделение строкового/одномерных/двумерных массивов
- [ ] UMEM — уничтожение массивов по типу
- [ ] CURR — текущий размер массива в переменную
- [ ] MEMC $$ / MEMC 1M / MEMC 2K — остальные варианты очистки

### Приоритет 2 — расширение ввода/вывода
- [ ] DATA A;B;C — заполнение строкового массива
- [ ] KEYS A — код нажатой клавиши без ожидания
- [ ] WAIT A — ожидание нажатия клавиши

### Приоритет 3 — строковые операции
- [ ] %MID(P,I,N), %CHR(N,S), %DATE, %TIME — строковые функции
- [ ] Присваивание строк: A: (число), A:$?[i] (код символа), A#$0 (вычисление выражения)
- [ ] $A-$Z — буквенные строковые переменные (сейчас только $0-$9)

### Приоритет 4 — файловые операторы (9 шт.)
- [ ] LENF, UDAL, PATH, FILE, FREE, READ, TYPE, GETF, PUTF

### Приоритет 5 — графика и системные вызовы
- [ ] GRAF/TEXT — реальный переход в режимы (сейчас заглушки)
- [ ] DOSC — вызов внешних программ/команд
- [ ] JULD — юлианский день (календарный оператор)

## Технический долг
- [ ] Написать unit-тесты для execAssignment, execEsli, expr.evaluate
- [ ] Проверить поведение MAX_LINE_LEN=75 в байтах vs символах для не-ASCII строк кода
- [ ] Добавить обработку ошибок в main.zig с корректными кодами возврата процесса
